// 7-9_JS,Iterators,Iterator-Documentation
/* ======================================
ESERCIZIO 1: Usa il metodo .map()
Hai un array chiamato numbers che contiene i numeri da 1 a 5.
Crea un nuovo array chiamato doubledNumbers usando il metodo .map().
Moltiplica ogni numero nell'array numbers per 2 e salva il risultato in doubledNumbers.
Alla fine, stampa nella console entrambi gli array numbers e doubledNumbers.
Dovresti vedere che numbers rimane invariato: [1, 2, 3, 4, 5]
e doubledNumbers mostra ogni numero moltiplicato per 2: [2, 4, 6, 8, 10]
====================================== */

// const numbers = [1, 2, 3, 4, 5];

// Crea doubledNumbers moltiplicando ogni elemento di numbers per 2 usando .map()

// Stampa numbers (deve mostrare: [1, 2, 3, 4, 5])
// Stampa doubledNumbers (deve mostrare: [2, 4, 6, 8, 10])

/* ======================================
ESERCIZIO 2: Filtra parole corte con .filter()
Hai un array chiamato words con alcune parole.
Crea un nuovo array chiamato shortWords usando il metodo .filter().
In shortWords salva solo le parole con meno di 5 caratteri.
Alla fine, stampa nella console entrambi gli array words e shortWords.
words deve rimanere invariato, ad esempio: ['apple', 'dog', 'cat', 'banana', 'bat']
shortWords deve contenere solo parole con meno di 5 caratteri: ['dog', 'cat', 'bat']
====================================== */

// const words = ['apple', 'dog', 'cat', 'banana', 'bat'];

// Usa .filter() per creare shortWords con parole lunghe meno di 5 caratteri

// Stampa words (deve mostrare: ['apple', 'dog', 'cat', 'banana', 'bat'])
// Stampa shortWords (deve mostrare: ['dog', 'cat', 'bat'])
