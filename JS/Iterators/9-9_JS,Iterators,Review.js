// 9-9_JS,Iterators,Review
/* ======================================
ESERCIZIO 1: Stampare ogni voce con .forEach()
Hai un array chiamato `animals` che contiene i seguenti nomi di animali:
["cat", "dog", "rabbit"]

Utilizza il metodo `.forEach()` per stampare nella console ogni nome di animale preceduto dalla stringa "Animal: ".

Risultato atteso nella console:
Animal: cat  
Animal: dog  
Animal: rabbit

1. Crea un array `animals` con i valori sopra indicati.
2. Usa `.forEach()` per iterare sull’array.
3. All'interno della funzione di callback, stampa ogni nome di animale con il prefisso richiesto.

Esegui il codice e controlla che la console mostri ogni animale correttamente formattato.
====================================== */


/* ======================================
ESERCIZIO 2: Calcolare il totale con .reduce()
Hai un array chiamato `expenses` che contiene i seguenti valori numerici:
[50, 20, 30]

Utilizza il metodo `.reduce()` per calcolare la somma totale delle spese e salvala nella variabile `totalExpenses`.

Risultato atteso nella console:
100

1. Crea un array `expenses` con i valori sopra indicati.
2. Usa `.reduce()` per sommare tutti i numeri dell’array.
3. Salva il valore ritornato da `.reduce()` nella costante `totalExpenses`.
4. Stampa `totalExpenses` nella console.

Esegui il codice e controlla che venga stampata la somma corretta.
====================================== */
