// 4-9_JS,Iterators,The-filter-Method
/* ======================================
ESERCIZIO 1: Filtrare le parole brevi

1. Crea un array chiamato `items` con i seguenti valori: "table", "cup", "bottle", "lamp", "box", "mirror".
2. Crea una costante `shortItems` e assegna il risultato di `.filter()` chiamato su `items`.
3. La funzione freccia deve restituire `true` solo per gli elementi con meno di 5 caratteri.
   // Risultato atteso per shortItems: ["cup", "lamp", "box"]
4. Stampa sia `items` che `shortItems` nella console.

Salva il file, esegui il codice e verifica l'output.
====================================== */

/* ======================================
ESERCIZIO 2: Filtrare i numeri positivi

1. Crea un array chiamato `temperatures` con i seguenti numeri: -5, 10, -3, 0, 7, -8, 15.
2. Crea una costante `positiveTemperatures` e assegna il risultato di `.filter()` chiamato su `temperatures`.
3. La funzione freccia deve restituire `true` solo per i numeri maggiori di 0.
   // Risultato atteso per positiveTemperatures: [10, 7, 15]
4. Stampa sia `temperatures` che `positiveTemperatures` nella console.

Salva il file, esegui il codice e verifica l'output.
====================================== */
