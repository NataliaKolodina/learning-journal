// 9-12_JS,Arrays,More-Array-Methods

/* ======================================
🔸 Esercizio 1: Usa .join(), .slice() e .concat() sugli array

1. Crea un array `groceries` con const contenente esattamente questi elementi in quest’ordine:
   'milk', 'bread', 'eggs', 'butter', 'cheese'.
2. Usa il metodo .join() su `groceries` per creare una stringa con tutti gli elementi separati da una virgola seguita da uno spazio.
3. Stampa la stringa risultante nella console.
   // Output atteso: "milk, bread, eggs, butter, cheese"
4. Usa il metodo .slice() per creare un nuovo array chiamato `morningGroceries` contenente solo i primi tre elementi di `groceries`.
5. Stampa `morningGroceries` nella console.
   // Output atteso: ['milk', 'bread', 'eggs']
6. Crea un altro array chiamato `additionalGroceries` contenente 'coffee' e 'tea'.
7. Usa .concat() per combinare `morningGroceries` e `additionalGroceries` in un nuovo array chiamato `fullList`.
8. Stampa `fullList` nella console.
   // Output atteso: ['milk', 'bread', 'eggs', 'coffee', 'tea']

Salva le modifiche, aggiorna la pagina ed esegui il codice per vedere il risultato.
====================================== */

/* ======================================
🔸 Esercizio 2: Usa .splice(), .shift() e .unshift() sugli array

1. Crea un array `snacks` con let contenente questi elementi in quest’ordine:
   'chips', 'cookies', 'popcorn', 'nuts'.
2. Usa .splice() per rimuovere il secondo elemento ('cookies') e inserire in quella posizione 'granola bar' e 'fruit snack'.
3. Stampa l’array `snacks` aggiornato nella console.
   // Output atteso: ['chips', 'granola bar', 'fruit snack', 'popcorn', 'nuts']
4. Usa .shift() per rimuovere il primo elemento di `snacks` e memorizzarlo in una variabile chiamata `firstSnack`.
5. Stampa `snacks` e `firstSnack` nella console, ciascuno su una riga separata.
   // Prima riga (array aggiornato): ['granola bar', 'fruit snack', 'popcorn', 'nuts']
   // Seconda riga (elemento rimosso): 'chips'
6. Usa .unshift() per aggiungere l’elemento 'pretzels' all’inizio dell’array `snacks`.
7. Stampa l’array finale `snacks` nella console.
   // Output atteso: ['pretzels', 'granola bar', 'fruit snack', 'popcorn', 'nuts']

Salva le modifiche, aggiorna la pagina ed esegui il codice per vedere il risultato.
====================================== */






