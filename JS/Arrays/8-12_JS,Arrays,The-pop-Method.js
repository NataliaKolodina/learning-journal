// 8-12_JS,Arrays,The-pop-Method

/* ======================================
🔸 Esercizio 1: Rimuovi l’ultimo elemento con .pop()
1. Crea un array `fruits` con const e inserisci esattamente questi elementi in quest’ordine:
   'apple', 'banana', 'cherry', 'date'.
2. Usa il metodo .pop() sull’array `fruits` e memorizza l’elemento rimosso in una variabile chiamata `lastFruit`.
3. Stampa l’array `fruits` aggiornato nella console.
   // Output atteso: ['apple', 'banana', 'cherry']
4. Stampa la variabile `lastFruit` nella console.
   // Output atteso: 'date'

Salva le modifiche, aggiorna la pagina ed esegui il codice per vedere il risultato.
====================================== */

/* ======================================
🔸 Esercizio 2: Usa .pop() due volte e memorizza gli elementi rimossi
1. Crea un array `colors` con const e inserisci queste stringhe in quest’ordine:
   'red', 'green', 'blue', 'yellow', 'purple'.
2. Usa il metodo .pop() due volte sull’array `colors`:
   - La prima chiamata deve memorizzare l’elemento rimosso in una variabile chiamata `removedFirst`.
   - La seconda chiamata deve memorizzare l’elemento rimosso in una variabile chiamata `removedSecond`.
3. Stampa l’array `colors` aggiornato nella console.
   // Output atteso: ['red', 'green', 'blue']
4. Stampa `removedFirst` e `removedSecond` nella console, ciascuno su una riga separata.
   // Output prima riga: 'purple'
   // Output seconda riga: 'yellow'

Salva le modifiche, aggiorna la pagina ed esegui il codice per vedere il risultato.
====================================== */




