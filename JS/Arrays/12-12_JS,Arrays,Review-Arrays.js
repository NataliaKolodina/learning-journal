// 12-12_JS,Arrays,Review-Arrays

/* ======================================
🔸 Esercizio 1: Crea e manipola un array base

1. Dichiara un array costante chiamato `colors` con esattamente quattro elementi stringa:
   - "red"
   - "blue"
   - "green"
   - "yellow"
2. Stampa nella console la lunghezza dell’array `colors`.
   // Output atteso: 4
3. Accedi e stampa nella console il terzo elemento dell’array `colors`.
   // Output atteso: green
4. Modifica il secondo elemento dell’array `colors` con la stringa "purple".
5. Stampa l’intero array `colors` per verificare la modifica.
   // Output atteso: ['red', 'purple', 'green', 'yellow']

Salva le modifiche, aggiorna la pagina ed esegui il codice per vedere il risultato.
====================================== */

/* ======================================
🔸 Esercizio 2: Metodi degli array e array annidati

1. Crea un array con let chiamato `numbers` inizializzato con gli elementi 10, 20 e 30.
2. Usa un metodo mutante per aggiungere il numero 40 alla fine dell’array `numbers`.
3. Usa un metodo mutante per rimuovere il primo elemento dell’array `numbers`.
4. Stampa l’array `numbers` per verificare le modifiche.
   // Output atteso: [20, 30, 40]
5. Crea un array costante annidato chiamato `nestedNumbers` che contiene:
   - Un array con gli elementi 1 e 2
   - Un array con gli elementi 3 e 4
6. Stampa nella console il primo array annidato dentro `nestedNumbers`.
   // Output atteso: [1, 2]
7. Stampa nella console il secondo elemento del secondo array annidato dentro `nestedNumbers`.
   // Output atteso: 4

Salva le modifiche, aggiorna la pagina ed esegui il codice per vedere il risultato.
====================================== */






