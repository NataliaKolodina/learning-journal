// 10-12_JS,Arrays,Arrays-and-Functions 

/* ======================================
🔸 Esercizio 1: Modificare un array all’interno di una funzione

1. Crea un array `colors` con const contenente esattamente queste tre stringhe in quest’ordine:
   'red', 'green', 'blue'.
2. Definisci una funzione chiamata `addColor` che accetta un parametro chiamato `arr`.
3. All’interno della funzione, usa il metodo `.push()` sul parametro `arr` per aggiungere la stringa 'yellow'.
4. Chiama la funzione `addColor` passando l’array `colors` come argomento.
5. Stampa l’array `colors` nella console.
   // Output atteso: ['red', 'green', 'blue', 'yellow']

Salva le modifiche, aggiorna la pagina ed esegui il codice per vedere il risultato.
====================================== */

/* ======================================
🔸 Esercizio 2: Mutazioni multiple di un array all’interno di funzioni

1. Crea un array `numbers` con let contenente questi cinque numeri in quest’ordine:
   1, 2, 3, 4, 5.
2. Definisci una funzione chiamata `removeLast` con un parametro chiamato `arr`.
3. All’interno della funzione `removeLast`, usa il metodo `.pop()` per rimuovere l’ultimo elemento di `arr`.
4. Definisci un’altra funzione chiamata `addNumber` con due parametri: `arr` e `num`.
5. All’interno di `addNumber`, usa `.push()` per aggiungere il valore `num` alla fine di `arr`.
6. Chiama `removeLast` passando l’array `numbers`.
7. Chiama `addNumber` passando l’array `numbers` e il numero 10.
8. Stampa l’array `numbers` nella console.
   // Output atteso: [1, 2, 3, 4, 10]

Salva le modifiche, aggiorna la pagina ed esegui il codice per vedere il risultato.
====================================== */






