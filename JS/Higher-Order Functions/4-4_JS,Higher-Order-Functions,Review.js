// 4-4_JS,Higher-Order-Functions,Review

/* ======================================
ESERCIZIO 1: Usare funzioni come dati

1. Crea una funzione chiamata `greet` che restituisce la stringa "Ciao!".
2. Assegna la funzione `greet` a una nuova variabile chiamata `saluto`.
3. Chiama la funzione `saluto` e mostra il risultato.
   // Risultato atteso: "Ciao!"

Salva il file, esegui il codice e verifica l'output.

// ====================================== */

/* ======================================
ESERCIZIO 2: Creare una funzione di ordine superiore

1. Definisci una funzione chiamata `callFunction` che accetta una funzione come parametro `fn`.
2. All'interno di `callFunction`, invoca la funzione `fn`.
3. Definisci una funzione chiamata `sayHi` che restituisce "Ciao dal callback!".
4. Passa `sayHi` a `callFunction` e mostra il risultato restituito.
   // Risultato atteso: "Ciao dal callback!"

Salva il file, esegui il codice e verifica l'output.
====================================== */
