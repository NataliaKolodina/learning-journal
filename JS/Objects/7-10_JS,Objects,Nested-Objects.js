// 7-10_JS,Objects,Nested-Objects
/* ======================================
ESERCIZIO: Accedere a proprietà annidate all'interno di un oggetto

1. Crea una costante `labUnit` e assegnale un oggetto con la seguente struttura:
   - Una proprietà `equipment`, che è un oggetto contenente:
     - Una proprietà `microscope`, che è un oggetto con:
       - `model`: il valore `'ZoomMax'`
       - `lens`: il valore `'UltraSharp'`
   - Una proprietà `technicians`, che è un oggetto contenente:
     - Una proprietà `lead`, che è un oggetto con:
       - `name`: il valore `'Marco'`
       - `field`: il valore `'Biology'`
2. Assegna a una nuova costante `leadField` il valore della proprietà `field` dell'oggetto `lead`.
3. Stampa `leadField` nella console.
   // Risultato atteso nella console:
   // "Biology"
====================================== */

/* ======================================
ESERCIZIO: Accedere a proprietà annidate con notazione a parentesi quadre

1. Aggiungi a `labUnit` una proprietà `storage`, che è un oggetto contenente:
   - Una proprietà `'cold-room'`, che è un oggetto con:
     - `temperature`: con valore `-20`
     - `status`: con valore `'active'`
2. Assegna a una nuova costante `coldRoomTemp` il valore della proprietà `temperature` dell’oggetto `'cold-room'` usando la notazione a parentesi quadre.
3. Stampa `coldRoomTemp` nella console.
   // Risultato atteso nella console:
   // -20
====================================== */
