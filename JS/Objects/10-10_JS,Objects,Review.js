// 10-10_JS,Objects,Review
/* ======================================
ESERCIZIO 1: Oggetti in JavaScript
In questo esercizio metterai in pratica i concetti chiave sugli oggetti:

1. Crea un oggetto chiamato `robot` con le seguenti proprietà:
   - model: 'RX-78'
   - energyLevel: 85
   - status: funzione che stampa "Sistema operativo attivo"
2. Accedi al valore della proprietà `model` usando la notazione a punti e assegnalo a una variabile `robotModel`.
   - Stampa `robotModel` in console.
3. Aggiungi una nuova proprietà `origin` usando la notazione a parentesi quadre. Il valore deve essere la stringa: 'Giappone'.
4. Modifica il valore della proprietà `energyLevel` a 100.
5. Cancella la proprietà `model` usando l'operatore `delete`.
6. Usa un ciclo `for...in` per stampare tutte le proprietà (chiavi) presenti nell’oggetto `robot`.
7. Attenzione: non confondere l’accesso o la modifica delle proprietà (mutazione) con la creazione di un nuovo oggetto.
   Ricorda: anche se un oggetto è dichiarato con `const`, le sue proprietà possono comunque essere mutate.
   // Risultato atteso (in console):
   // RX-78
   // status
   // energyLevel
   // origin
8. Esegui il codice per verificare il comportamento.
====================================== */

/* ======================================
ESERCIZIO 2: Oggetto 'student'

1. Crea un oggetto `student` con queste proprietà:
   - name: 'Giulia'
   - age: 21
   - greet: funzione che stampa "Ciao, piacere di conoscerti!"
2. Assegna la proprietà `name` a una variabile `studentName` usando la notazione a punti.
   - Stampa `studentName` in console.
3. Aggiungi una nuova proprietà `university` con valore 'Università di Bologna' usando la notazione tra parentesi quadre.
4. Modifica la proprietà `age` in 22.
5. Elimina la proprietà `greet`.
6. Usa un ciclo `for...in` per stampare i nomi di tutte le proprietà dell’oggetto `student`.
   // Risultato atteso (in console):
   // Giulia
   // name
   // age
   // university
====================================== */
