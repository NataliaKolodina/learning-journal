// 2-10_JS,Loops,Repeating-Tasks-Manually

/* ======================================
EXERCISE 1: Simula una Ripetizione Manuale
Scrivi uno script che simuli una ripetizione manuale di un’istruzione stampando il messaggio "Repetition step" cinque volte, senza usare cicli.

1. Usa `console.log()` per stampare la stringa "Repetition step".
2. Ripeti esattamente questa istruzione cinque volte, una sotto l’altra, senza usare alcun ciclo, funzione o condizione.

Risultato atteso:
"Repetition step"  
"Repetition step"  
"Repetition step"  
"Repetition step"  
"Repetition step"

Salva le modifiche, ricarica la pagina e verifica l’output nella console.
====================================== */

/* ======================================
EXERCISE 2: Usa un ciclo `for` per evitare la ripetizione manuale
Scrivi uno script che stampi lo stesso risultato dell’esercizio 1, ma usando un ciclo `for` invece di ripetere manualmente le istruzioni.

1. Crea una variabile `step` inizializzata a 0 usando `let`.
2. Usa un ciclo `for` che inizia con `step = 0`, termina quando `step` è minore di 5, e incrementa `step` di 1 ad ogni iterazione.
3. All’interno del corpo del ciclo, usa `console.log()` per stampare la stringa "Repetition step".

Risultato atteso:
"Repetition step"  
"Repetition step"  
"Repetition step"  
"Repetition step"  
"Repetition step"

Salva le modifiche, ricarica la pagina e verifica l’output nella console.
====================================== */


