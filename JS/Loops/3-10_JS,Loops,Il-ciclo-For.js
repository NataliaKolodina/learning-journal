// 3-10_JS,Loops,Il-ciclo-For

/* ======================================
EXERCISE 1: Stampa numeri da 0 a 4 con un ciclo `for`
Scrivi uno script che usi un ciclo `for` per stampare i numeri da 0 a 4 (incluso 0, escluso 5) nella console.

1. Dichiara una variabile `index` all’interno del ciclo `for`, inizializzandola a 0.
2. Imposta come condizione che `index` sia minore di 5.
3. Dopo ogni ciclo, incrementa `index` di 1.
4. All’interno del corpo del ciclo, usa `console.log()` per stampare il valore della variabile `index`.

Risultato atteso:
0  
1  
2  
3  
4

Salva le modifiche, ricarica la pagina e verifica l’output nella console.
====================================== */

/* ======================================
EXERCISE 2: Usa un ciclo `for` per stampare solo numeri pari da 2 a 10
Scrivi uno script che stampi **solo i numeri pari** da 2 a 10 (incluso) usando un ciclo `for`.

1. Dichiara una variabile `evenNumber` all’interno del ciclo `for`, inizializzandola a 2.
2. Imposta come condizione che `evenNumber` sia minore o uguale a 10.
3. Dopo ogni ciclo, incrementa `evenNumber` di 2.
4. All’interno del corpo del ciclo, usa `console.log()` per stampare il valore della variabile `evenNumber`.

Risultato atteso:
2  
4  
6  
8  
10

Salva le modifiche, ricarica la pagina e verifica l’output nella console.
====================================== */



