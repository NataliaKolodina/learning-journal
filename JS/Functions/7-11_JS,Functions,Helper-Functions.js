// 7-11_JS,Functions,Helper-Functions
/* ======================================
EXERCISE 1: Usare una funzione helper per completare un calcolo
1. Crea una funzione chiamata `calculateSquare` che prende un parametro `number` e restituisce `number` moltiplicato per sé stesso.
2. Crea un’altra funzione chiamata `sumOfSquares` che prende due parametri: `a` e `b`.
3. All’interno di `sumOfSquares`, chiama `calculateSquare` due volte: una con `a`, una con `b`.
4. Somma i risultati e restituisci il totale.
5. Chiama `sumOfSquares` con i valori `3` e `4`, e stampa il risultato nella console.
   // 25
====================================== */
/* ======================================
EXERCISE 2: Combinare più funzioni helper
1. Crea una funzione `addTax` che prende un prezzo come parametro e restituisce il prezzo con un aumento del 10%.
2. Crea una funzione `applyDiscount` che prende un prezzo come parametro e restituisce il prezzo con una riduzione del 20%.
3. Crea una funzione `getFinalPrice` che prende un parametro `initialPrice`.
4. All’interno di `getFinalPrice`, chiama prima `addTax`, poi passa il risultato ad `applyDiscount`.
5. Restituisci il valore finale da `getFinalPrice`.
6. Chiama `getFinalPrice` con l’argomento `100` e stampa il risultato nella console.
   // 88
====================================== */


