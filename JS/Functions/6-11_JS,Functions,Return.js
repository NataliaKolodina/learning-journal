// 6-11_JS,Functions,Return
/* ======================================
EXERCISE 1: Usare il return per calcolare un risultato
1. Crea una funzione chiamata `multiplyByTwo` che prende un parametro chiamato `number`.
2. All’interno della funzione, restituisci il valore di `number` moltiplicato per 2.
3. Chiama la funzione con l’argomento `6` e salva il risultato in una variabile chiamata `result`.
4. Stampa `result` nella console.
   // 12
====================================== */
/* ======================================
EXERCISE 2: Usare il return condizionale
1. Crea una funzione chiamata `calculateDiscountedPrice` con due parametri: `price` e `discount`.
2. Se `price` è inferiore a 0 o `discount` è inferiore a 0, restituisci la stringa: "Invalid input".
3. Altrimenti, restituisci il prezzo dopo aver applicato lo sconto, utilizzando la formula:
   `price - (price * discount)`.
4. Chiama la funzione con gli argomenti `100` e `0.2`, e salva il risultato in una variabile chiamata `finalPrice`.
5. Stampa `finalPrice` nella console.
   // 80
6. Chiama la funzione con gli argomenti `-50` e `0.1` e stampa direttamente il risultato.
   // Invalid input
====================================== */


