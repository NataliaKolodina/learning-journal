// 11-11_JS,Functions,Review
/* ======================================
ESERCIZIO 1: Dichiarare e Richiamare Funzioni
1. All'inizio dello script, dichiara una funzione chiamata `showMessage` che non accetta parametri.
2. All'interno del corpo della funzione, stampa in console la stringa: "JavaScript è divertente!"  
   // Risultato atteso: JavaScript è divertente!
3. Dopo la dichiarazione, richiama la funzione `showMessage`.
Salva il file, esegui il codice e controlla il risultato nella console.
====================================== */
/* ======================================
ESERCIZIO 2: Parametri, Valori di Default, Return e Funzioni Freccia
1. Crea una funzione freccia chiamata `calculateDiscount` che accetta due parametri: `price` e `discount`, con `discount` impostato di default a 10.
2. La funzione deve restituire il prezzo scontato calcolato come: `price - (price * discount / 100)`.
3. Crea una seconda funzione chiamata `printFinalPrice` che accetta un parametro `finalPrice` e stampa in console: "Il prezzo finale è: [valore]"  
   // Risultato atteso: Il prezzo finale è: 90 (se `price = 100` e `discount` non viene specificato)
4. All'interno del tuo script, richiama `calculateDiscount` con un solo argomento: 100, e passa il risultato direttamente a `printFinalPrice`.
Salva il file, esegui il codice e controlla il risultato nella console.
====================================== */
